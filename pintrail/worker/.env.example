# Database (PostgreSQL) — must match pt2
DB_HOST=localhost
DB_PORT=5432
DB_USER=pintrail
DB_PASSWORD=pintrail
DB_NAME=pintrail

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
IMAGE_QUEUE_NAME=artifact-image-processing

# Storage — must point to the same volume as pt2
IMAGE_STORAGE_ROOT=./data/images
